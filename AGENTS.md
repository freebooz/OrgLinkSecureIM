# OrgLinkSecureIM 工程约束

## 产品与命名

- 产品中文名为“安信通”，英文名为 “OrgLink Secure IM”。
- 工程名统一使用 `OrgLinkSecureIM`，C++ 根命名空间统一使用 `orglink`。
- 环境变量统一使用 `ORGLINK_` 前缀。

## 架构边界

- 组织、部门、岗位、人员是通信能力的业务基座；人员、账号、设备必须独立建模。
- 客户端遵循 MVC + Service + Repository：View 不得访问网络、数据库或解析协议；Model 不得依赖 QWidget；Controller 只编排用例。
- `libs/domain` 必须保持纯 C++，不得依赖 Qt、数据库驱动、网络框架或具体密码库。
- 平台代码与业务代码隔离；信创平台兼容性未经实机 POC 不得标注为已验证。

## 桌面窗口与托盘全局策略

- Windows/Linux 桌面端普通关闭必须缩小到系统托盘并保持安全连接；只有托盘菜单“退出安信通”执行真正退出。系统托盘不可用时必须降级为普通关闭，避免产生无法恢复的后台进程。
- 收到服务端实时消息且主窗口未处于前台时，托盘图标必须闪烁；恢复或激活主窗口后停止闪烁。托盘通知不得显示发送者或消息正文。
- Android/iOS 不使用桌面托盘策略，必须遵循各自平台应用生命周期。

## 中文注释与安全

- 新增或修改的类、枚举、成员和公共方法必须使用准确中文注释说明职责、约束、副作用和失败条件。
- 关键状态转换、事务、幂等、权限、安全校验、资源释放与异常回退必须解释设计原因。
- 禁止在源码、模拟数据、日志或安装包中写入真实密码、私钥、测试证书和访问令牌。
- 运行数据、日志、下载文件和聊天缓存不得写入程序安装目录。

## 质量门禁

- 变更后至少执行 CMake 配置、编译与 CTest；Qt 不可用时必须说明未运行的 Qt 测试。
- 禁止用空类、空函数或固定返回值冒充完成的功能；报告必须区分“已验证”“未验证”“仅骨架”和“未实现”。
- 所有数据库变更使用迁移文件；组织数据变更必须产生修订号和审计记录。
